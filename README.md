# Тріаж тікетів і асистент агента

Дві системи над одним продуктом — [Nebula](https://www.asknebula.com), застосунок
духовних консультацій:

1. **Тріаж тікета** — на вхід текст звернення, на вихід категорія, пріоритет і
   наступний крок.
2. **Асистент агента** — на вхід текст звернення, на вихід саммарі, три
   варіанти відповіді різного тону й цитата з бази знань.

Обидві побудовані на справжніх даних, а не на вигаданих прикладах: 43 941
зібраний відгук, 417 розмічених руками тікетів і 84 власні опубліковані
документи компанії як база знань.

**Демонстрація:** [obrio-support-triage.fly.dev](https://obrio-support-triage.fly.dev)
— одна сторінка, перемикач зверху, тріаж відкривається першим.

## Що вміє система

Тріаж:

| що зроблено | де подивитись | що це означає |
|---|---|---|
| текст → категорія, пріоритет, крок | вкладка «Таск 1» | Працює одна заморожена пара: промпт `v4` і модель `gemini-3.1-flash-lite`. «Заморожена» означає, що обидві названі в одному місці — [`app/config.py`](app/config.py) — і всі числа нижче зміряні саме на ній. |
| визначення категорій | [`app/taxonomy.py`](app/taxonomy.py) — словник для моделі; [`data/tickets/LABELLING.md`](data/tickets/LABELLING.md) — правила розмітки | Шість категорій, і для кожної записано три речі: яку частку тікетів вона займає, одне вирішальне питання, що відрізняє її від сусідньої, і перелік наступних кроків, дозволених саме для неї. Модель і людина-розмітник читають однакові визначення. |
| стійкість до ворожого вводу | [`data/sets/synthetic_v1.json`](data/sets/synthetic_v1.json), результат у [`data/runs/`](data/runs/) | П'ятнадцять навмисно складних тікетів: лайка, дві теми в одному зверненні, спроба перехопити інструкцію, порожній текст, дуже довгий текст, вісім мов. Усі три мітки правильні в **дев'яти з п'ятнадцяти**. |
| вибір моделі | [`eval/benchmark.py`](eval/benchmark.py) — склад і правило вибору; таблиці в [`data/runs/`](data/runs/) | Десять моделей на тих самих тікетах і з тим самим промптом. Правило, за яким обирається переможець, записане в код **до** першого запиту — щоб його не можна було підігнати під результат, який сподобався. |
| поведінка при збоях провайдера | докстрінг [`app/llm.py`](app/llm.py) | Зламаний JSON у відповіді ремонтують, а не викидають разом із тікетом. На «забагато запитів» чекають рівно стільки, скільки просить сервер. Обмеження часу стоїть на тікет, а не на окрему спробу, тож три повтори не перетворюються на потрійне очікування. |
| передача людині | `REVIEW_RULES` у [`app/taxonomy.py`](app/taxonomy.py) | Систему не питають, наскільки вона впевнена. Її відповідь проганяють через п'ять фіксованих правил, і якщо спрацювало хоч одне — тікет іде до людини. Два з правил не про помилку, а про зміст: `P1` і є та черга, яку людина дивиться першою, а `other` означає, що модель не знайшла жодної відповідної категорії. Три інші ловлять самосуперечність: крок, заборонений для цієї категорії; пріоритет, що не випливає з фактів, які модель сама ж і склала; цитата, якої в тікеті немає. Поріг впевненості тут не працює — обрана модель у середньому дає 0,997, коли має рацію, і 0,994, коли помиляється. |
| історія промпту | [`docs/TASK_1.md`](docs/TASK_1.md), джерела — [`prompts/classify/`](prompts/classify/) і поле `change_reason` у [`app/config.py`](app/config.py) | Сім версій. Для кожної заздалегідь записано, що саме змінюється і якого ефекту від цього чекають. Запис зроблено **до** прогону, тож видно й ті здогадки, які не справдились. |
| заміри й розбір помилок | [`data/runs/*.table.md`](data/runs/) і [`eval/run.py`](eval/run.py) | Набори заморожені, тому два прогони можна чесно порівняти між собою. Для кожного є таблиця `вхід → очікуване → фактичне → pass/fail`, матриці плутанини і розбивка по мовах. Останній замір на недоторканому наборі `golden`: головне число **0,852**, а всі три мітки одночасно правильні в **151 тікеті з 235**. |
| порівняння моделей: якість, швидкість, ціна | [`data/runs/benchmark_*.md`](data/runs/) | Два раунди. Спершу дешевий відсів на 50 випадкових тікетах, щоб не платити за явних аутсайдерів. Потім повне ранжування тих, хто пройшов, на всіх 157. |
| вартість | [`data/runs/eval_results.csv`](data/runs/eval_results.csv) | **$0,00087 за тікет**, або **$8,65 за десять тисяч**. Це фактично спожиті токени з відповіді провайдера, а не оцінка за прайс-листом: вони розходяться, бо частина моделей додатково тарифікує «міркування», яких ніхто не просив. |

Асистент:

| можливість                                         | де                                                                                                                  | коротко                                                                                                                           |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| саммарі, три тони, цитата з бази    | вкладка «Таск 2»; або`POST /draft`                                                                  | `assist/v3` + `gemini-3.1-flash-lite`                                                                                                |
| як досягнуто різних тонів              | [`docs/TASK_2.md`](docs/TASK_2.md), [`prompts/assist/`](prompts/assist/), [`prompts/localise/`](prompts/localise/) | три версії генерації + окремий промпт локалізації                                              |
| що ламалось і як полагоджено         | [`docs/TASK_2.md`](docs/TASK_2.md)                                                                                   | запобіжник позначив як порушення власне речення політики                               |
| межі автоматизації                          | [`docs/AUTOMATION_BOUNDARIES.md`](docs/AUTOMATION_BOUNDARIES.md)                                                     | сім сценаріїв, два незалежні шари; на кризі модель не викликається взагалі |
| коли судження людини обов'язкове | [`docs/TASK_2.md`](docs/TASK_2.md), [`docs/AUTOMATION_BOUNDARIES.md`](docs/AUTOMATION_BOUNDARIES.md)                | криза, неповнолітній, юридична сторона — детерміністично, до моделі             |
| бенчмарк пошуку по базі знань | [`eval/results/retrieval_20260922T025458Z.json`](eval/results/), опис сітки — [`benchmark/README.md`](benchmark/README.md) | Щоб дати моделі потрібний документ, його спершу треба знайти, а способів зробити це багато. Порівняли 14: два способи різати документи на шматки, помножені на сім способів шукати — три моделі-кодувальники окремо, ті самі три в парі з класичним пошуком за словами, і пошук за словами сам по собі. Усі на тих самих 422 тікетах, де правильний документ відомий заздалегідь. Спосіб, що працює в системі, ставить потрібний документ **першим у 61%** випадків і **в перші п'ять — у 93%**; навмання це було б 4% і 19%. Обрано при цьому не найточніший на першій позиції: індекс, різаний по секціях, трохи випереджає, але впевнено підсовує інструкцію зі скасування не для того магазину, тому взято той, що тримає всі шість варіантів в одному документі. |

Розділ 3 у [`docs/TASK_1.md`](docs/TASK_1.md) перелічує рішення, ухвалені людиною,
а не моделлю: архітектура замірів і критерії відбору даних.

**Чого в репозиторії немає, і це варто знати наперед:** зведеного тексту «опис
архітектури» як окремого документа — обґрунтування живуть у докстрінгах модулів,
які вони описують, а `docs/ARCHITECTURE.md` описує будову каталогів і відстав
від коду. Для асистента не робилось порівняння моделей генерації: один прогін
однієї моделі на 20 чернетках.

## Стан

|                                     | стан                                                                                                                                                              |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| збір даних                 | зроблено — 43 941 відгук, 124 сторфронти App Store, 47 мовних шарів Google Play                                                   |
| база знань                 | зроблено — 84 документи зібрано, 73 в індексі, два варіанти чанкінгу                                              |
| розмітка тікетів     | зроблено — 417 тікетів: категорія, пріоритет, наступний крок; правила в`data/tickets/LABELLING.md`           |
| система тріажу         | зроблено — 7 версій промпту, 10 моделей на бенчмарку, заморожено`v4` + `gemini-3.1-flash-lite`                  |
| бенчмарк ретрівера | зроблено — 14 клітинок на 422 запитах, переможець`article` + dense + `text-embedding-3-small`                                 |
| асистент                    | зроблено — промпт`assist/v3`, `gemini-3.1-flash-lite`, два незалежні шари меж автоматизації                        |
| розгортання              | зроблено — образ 354 МБ, одна машина в fra, перевірка готовності читає поле, а не код відповіді |

Жодне твердження вище не зроблене без підстави в самому репозиторії: кожне
число породжене скриптом звідси, а таблиці у звітах генеруються з виводу
прогонів, а не набираються руками.

## Тріаж: що зміряно

Заморожена пара — промпт `v4` і `google/gemini-3.1-flash-lite`. На 157 рядках
набору `dev`, промпт однаковий для всіх моделей:

|                                       | macro-F1 ядро | усі класи | пріоритет | вигаданих цитат | $ / 10k        | p50                  |
| ------------------------------------- | ----------------- | ----------------- | ------------------ | ----------------------------- | -------------- | -------------------- |
| claude-sonnet-5                       | **0,838**   | 0,719             | 0,710              | 6                             | 145,16         | 7 608 мс           |
| gpt-5-mini                            | 0,819             | 0,709             | 0,757              | 9                             | 26,35          | 13 172 мс          |
| **gemini-3.1-flash-lite**       | 0,803             | **0,725**   | **0,786**    | **2**                   | **8,65** | **1 640 мс** |
| gpt-4o-mini (база промпту) | 0,775             | 0,642             | 0,700              | 12                            | 2,99           | 2 172 мс           |

Два головні числа дають різних лідерів — 0,035 в один бік і 0,006 в інший, —
тож за якістю це нічия. Вирішують осі, які не шумлять: пріоритет, цитати,
затримка і сімнадцятикратна різниця в ціні. Власну смугу розкиду для цих трьох
моделей не міряли, тому твердження «gemini краща за sonnet» не робиться.

Три речі, без яких таблиця читається неправильно:

- **Головне число — macro-F1, не точність.** Мітки перекошені: `refund_and_cancel`
  це 73,4% кроків, `P3` — 58,3% пріоритетів. Модель, що завжди відповідає
  найчастішим значенням, дає 73 / 42 / 58 і не розуміє нічого.
- **Власний шум методу — 0,0024.** Три прогони одного промпту. Приріст, менший
  за це, не приріст; правило записане в `eval/benchmark.py` до першого запиту.
- **Хибний факт терміновості на еталонних P3**: 35% у базової моделі, 6% в
  обраної. П'ять версій промпту не зрушили це число взагалі — зрушила зміна
  моделі. Це і є межа промпт-інжинірингу на виміряному прикладі.

Еволюція промпту: `prompts/classify/v1.md` … `v7.md`, причина кожної зміни — у
полі `change_reason` відповідного запису в `app/config.py`. Сирі відповіді,
зведення і таблиці `вхід → очікуване → фактичне → pass/fail` по кожному прогону
лежать у `data/runs/`.

## Як запустити

```bash
pip install -r requirements.txt
cp .env.example .env          # один ключ: OPENROUTER_API_KEY
```

Один ключ дістає до всіх моделей — і чат, і ембединги йдуть через OpenRouter,
який говорить у форматі OpenAI.

Сторінка з обома задачами — перемикач зверху, тріаж відкривається першим:

```bash
uvicorn app.api:app --reload        # http://127.0.0.1:8000
```

Тріаж без браузера — прогін по замороженому набору і бенчмарк моделей:

```bash
python eval/run.py --set dev --prompt v4            # прогін + метрики + рядок у журнал
python eval/run.py --set dev --prompt v4 --show 5   # показати, що пішло в модель і що прийшло
python eval/run.py --set dev --prompt v4 --repeats 3  # власний розкид методу
python eval/run.py --score data/runs/<файл>.jsonl   # перерахувати, без викликів API
python eval/benchmark.py --dry-run                  # план і кошторис, без витрат
python eval/benchmark.py --sample 0                 # усі моделі на всіх 157 рядках
```

Асистент — чернетки, межі автоматизації і бенчмарк ретрівера:

```bash
python eval/assist.py --limit 20                    # чернетки на 20 тікетах
python eval/assist.py --limit 20 --model <інша>     # те саме іншою моделлю
python eval/boundaries.py                           # 19 мусять спрацювати, 17 ні
python eval/retrieval.py                            # усі 14 клітинок сітки
python eval/retrieval.py --limit 40                 # швидкий прохід
```

Решта — дані й база знань:

```bash
python scripts/collect/scrape_helpcenter.py    # перезібрати базу знань
python scripts/build_kb.py                     # порізати, зембедити, записати індекс
python scripts/build_kb.py --dry-run           # розміри й ціна, без викликів API
python scripts/split_eval.py                   # перезібрати заморожені набори
python scripts/probe_models.py                 # які параметри й ціни в моделей зараз
python benchmark/pick.py                       # відібрати тікети для бенчмарку ретрівера
```

## Розгортання

```bash
npm --prefix web run build                     # web/dist комітиться, не збирається на сервері
docker build -t triage . && docker run -p 8080:8080 --env-file .env triage
```

На fly.io — `fly.toml` і `Dockerfile` лежать у корені:

```bash
fly apps create <ім'я> --org personal
fly secrets set OPENROUTER_API_KEY="..." --app <ім'я>    # ДО деплою: ключ потрібен на старті
fly deploy
fly scale count 1 --yes                                   # типово створюються дві машини
```

Чотири речі в цих файлах записані не за звичкою, а за виміром:

- **Один стейдж, не multi-stage.** Обидва варіанти зібрані поруч — 354 МБ
  проти 354 МБ. Викидати нічого: сім закріплених пакетів із готовими
  cp313-колесами, ні компілятора, ні apt-кешу, а кеш pip вимкнений.
- **256 МБ пам'яті плюс 256 МБ swap.** Процес із завантаженим індексом тримає
  50 MiB — 19,6% машини.
- **Один воркер і одна машина.** Денна стеля витрат і лічильник звернень
  живуть у пам'яті процесу; другий воркер вів би власну копію, і стеля тихо
  подвоїлась би. Перевірено на живому сервісі: дві машини відповідали на
  `/health` різними сумами витраченого.
- **`HEALTHCHECK` читає поле, а не код відповіді.** `/health` віддає 200
  навіть тоді, коли індекс не завантажився — про це він каже полем `ok`.
  Перевірка тільки за кодом назвала б зламаний контейнер живим.

Ключ живе лише як секрет платформи: `.env` у `.dockerignore`, в образі його
немає, у шарах — теж.

## Будова

```
app/          система, пласкі модулі, один концерн на файл
prompts/      промпти як файли з версіями: classify/ для тріажу, assist/ для відповідей
scripts/      запускні утиліти; scripts/collect/ збирає сирі дані
benchmark/    ВИБІР компонентів: чанкінг, енкодер, спосіб пошуку
eval/         ВИМІР системи, зібраної з обраних компонентів
web/          сторінка з обома задачами; dist/ закомічено, щоб деплою вистачило Python
data/         зібране, згенероване й розмічене руками — окремо одне від одного
docs/         рішення, які не видно з коду
```

Промпти лежать файлами, а не рядковими літералами в коді: версію видно в
`git log`, різницю між двома версіями — в `git diff`, а `app/config.py` тримає
поруч із кожною налаштування моделі й причину, чому вона з'явилась.

`benchmark/` і `eval/` розділені навмисно: перший обирає деталі, другий міряє
зібране ціле. `docs/ARCHITECTURE.md` перелічує кожен файл із рішенням, яке за
ним стоїть.

## Що таке ці дані

|                              |                                                                                                                                                                                                                                         |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data/corpus.jsonl`        | 43 941 відгук, PII знято при зборі. У gitignore: 38 МБ, і відтворюється з`data/collect/raw/`                                                                                                     |
| `data/collect/raw/`        | дампи як зібрані, 183 файли, не редагуються ніколи                                                                                                                                                |
| `data/tickets/tickets.csv` | 417 тікетів, розмічені руками; у`worklog/` — черги й рішення, з яких вони вийшли                                                                                                  |
| `data/sets/`               | заморожений розподіл цих тікетів:`dev` 157 для ітерацій, `golden` 235 + `synthetic` 15 для заміру, `fewshot` 25 всередину промпту                              |
| `data/runs/`               | кожен прогін: сирі відповіді, зведення метрик і таблиця`вхід → очікуване → фактичне → pass/fail`; `eval_results.csv` — один рядок на прогін |
| `data/kb/`                 | пошуковий індекс і його вектори, породжені`scripts/build_kb.py`                                                                                                                                   |

База знань — власна документація компанії: 51 стаття довідкового центру
Zendesk, 14 питань FAQ і 19 політик, кожна з перевіреною адресою. Вона
**англійська й тільки англійська** — `GET /help_center/locales.json` повертає
`["en-us"]` — тоді як 48% тікетів не англійські. Тобто кожен не-англійський
пошук є крос-мовним не за нашим вибором, а за даністю. Саме це рішення
бенчмарк ретрівера й існує, щоб ухвалити на доказах, а не на вподобанні.
